"""Pipeline drivers for the SC26 artifact.

These scripts regenerate the upstream sensitivity CSVs that feed the
figure-generation stage. They are optional: the default reviewer path
uses the precomputed CSVs shipped under ``data/``.
"""
