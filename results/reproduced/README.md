# Compact numerical results

This directory contains compact analysis outputs used by the reproduction
and plotting workflows.

- `fig3/` contains Monolithic-LP latency and bandwidth curves.
- `fig6/` contains Llama latency and bandwidth curves.

Additional compact inputs for Figures 4 and 5 are under
`local_artifact/results/`.
